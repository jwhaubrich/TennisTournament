public class Referee {

    /*
    Take two teams, match them, and put them into match object
        - createMatch
    Assign final score to
     */

    public static void createMatches(int numberOfTeams){
        int actualMatch;
        int matchNumber;
        actualMatch = 2/numberOfTeams;

        for(matchNumber = 1; actualMatch >= matchNumber; matchNumber++){

        }
    }
}
